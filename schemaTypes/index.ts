import { profile } from './profile';
import { project } from './project';
import { article } from './article';
import { track } from './track';

export const schemaTypes = [profile, project, article, track];